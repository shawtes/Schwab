# Backend Python Dependencies for Trading Platform
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
python-dotenv>=1.0.0

# Visualization (needed by stock_screener.py)
matplotlib>=3.7.0
seaborn>=0.12.0

# Schwab API client (should already be installed from parent directory)
# schwabdev (installed from parent directory)

# Machine Learning (needed by ensemble_trading_model.py)
scikit-learn>=1.3.0

# Optional but recommended
requests>=2.31.0
